pipeline {
    agent { label 'mac_agent' }

    stages {
        stage('clone code') {
            steps {
                git 'https://github.com/Danielyan86/git-practice.git'
            }
        }
    }
}
